

pluginManagement {
    repositories {
        mavenLocal() // Forces Gradle to look in ~/.m2/repository first
        gradlePluginPortal() // Fallback for community plugins
        mavenCentral()
    }
}



rootProject.name="watch-extra-files-poc"

